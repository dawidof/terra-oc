import { DeliveriesManager } from "@/components/crm/deliveries-manager";

export const metadata = {
  title: "Поставки — CRM — TerraAuto",
};

export default function DeliveriesPage() {
  return <DeliveriesManager />;
}
